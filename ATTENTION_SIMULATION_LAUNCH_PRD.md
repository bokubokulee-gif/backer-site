# 03 / Attention Simulation

Add a third research preview linking to the new public Backer simulation website. Preserve the existing Research robot, greeting toggle, orbit motion and two preview links. The preview grid has three equal columns at every viewport width. Existing cards share the first row; the next research preview starts a second row automatically. Labels may wrap within compact cards on narrow tabs, but cards do not change to a single-column layout. It uses the established Backer typography, peach accent and border treatment.

Title: Attention Simulation. Index: 03 /. Description: Watch 50 fictional people react to real public content across social platforms and prediction markets. All behavior is simulated.

The card supports pointer, keyboard and direct #simulation selection. Its public destination is research-lab/attention-simulation.html. This full-viewport Research page embeds the existing public simulation service, preserving its live API connections, interactive canvas, workers, and local run state. It does not redirect visitors to the service domain. The Backer wordmark returns the top-level window to this Research entry. The supplied attention-simulatoin.html spelling redirects to the canonical route. Publish the audited public Research files, then verify the canonical page and destination without a sign-in requirement.
