{
  "name": "backer-site",
  "version": "1.0.0",
  "private": true,
  "description": "Backer static demo with privacy-preserving first-party analytics.",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:globe": "node scripts/vendor-cobe.js",
    "lint": "node scripts/lint.js",
    "test": "node --test tests/core*.test.js tests/backend*.test.js",
    "test:backend": "node --test tests/backend*.test.js",
    "test:browser": "node --test tests/browser*.test.cjs",
    "test:core": "node --test tests/core*.test.js",
    "sync:github": "node scripts/sync-github-momentum.mjs",
    "sync:market2": "node scripts/sync-market2-people.mjs",
    "hash:admin-password": "node scripts/hash-admin-password.js"
  },
  "dependencies": {
    "cobe": "2.0.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
